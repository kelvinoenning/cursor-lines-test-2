function sum(a, b) {
    return a + b;
}

function subtract(a, b) {
    return a + b;
}

function multiply(a, b) {
    return a * b;
}

function divide(a, b) {
    return a / b;
}

function log(message) {
    console.log(message);
}

log(sum(1, 2));
log(subtract(4, 1));
log(divide(10, 2));
log(multiply(3, 4));